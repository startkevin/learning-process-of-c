一个关于对数器的初级介绍
实现思路：
1.首先需要确立你想要测的方法a（即最优解）
2.其次确立复杂度不好但是容易实现的方法b（暴力解）
3.实现一个随机样本产生器，长度和取值都随机。
4.将相同样本输入方法a和方法b中，看看其分别得出的结果是否相同。
5.如果有一个随机样本使得比对结果不一致，打印这个出错的样本进行人工干预，以此修正方法a和b
6.当样本数量达到较高值时而对比测试依旧正确，可以确定方法a已经正确。
总结：
第5步为核心要点，我们需要找到一个数据量小的错误样本，然后把错误例子带入样本一步步检查，print大法或者断点技术都可以。

一些关键代码的实现：
PART1：得到随机数组
#include<stdio.h>
#include<time.h> //调用关于时间的相关函数，在这里用作随机数种子
int*randomArray(int n,int v){  //  n———希望的数组长度（有多少个元素），v则为随机数的最大值
    int*arr=(int*)malloc(n*sizeof(int)); //用来储存数，因此大小为int
    for(int i=0;i<n;i++){ //遍历数组的每一个元素来填入相应的随机数值
       arr[i]=rand()%v+1; //注意：1.rand()是生成0-v(指定最大值)之间的随机整数 2.rand()%v生成的是0-(v-1),+1后变为了1-v    
    }
    return arr;//返回赋值完的址数组首地
}
     调用实例：（在main函数中实现）
     int main(){
***  srand(time(NULL));  //初始化随机数种子
     int*arr=randomArray(5,100);//创建随机数数组，eg.希望得到一个含有5个数，元素大小在0-100间的随机数组
     ......
     ......
     }
     特别介绍：————关于srand(time(NULL))
         Q1：为什么需要随机数种子？
         A： 计算机生成的其实是伪随机数（通过某种公式计算出来），若不设置种子，每次运行程序都会得到相同的随机数序列
         Q2：srand()是什么？
         A： 全称为seed random，作用是初始化随机数生成器，设置随机数序列起点，需要一个整数作为种子。
         Q3: time(NULL)是什么用法？
         A： time()是获取当下时间的函数，有两种主要用法：1.time(NULL)就是获取当前时间，返回值是一个很大的整数
                                                     2.time(&start)是获取当前时间，并储存到变量start中
     

