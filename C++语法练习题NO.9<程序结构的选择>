<关于怎么判断程序需要什么组成成分>：
  step1：分析数据特点——————>有多个对象需要管理，有则选择用类或者结构体。
         怎么区分是选类class还是结构体struct？看对象内部是否有深度绑定的行为（操作）或者说是内部的复杂逻辑？
         eg.就拿酒店房间来说，我们来区分一下什么叫做需要class,什么情况需要struct:
            行为：订房、入住、退房...这些并不是房间（对象）的行为或者内部逻辑，而是整个酒店（系统）的行为。————struct(存数据即可)
            行为：订房规则：如需验证时间、对于房价有关于特殊楼层或节假日的价格变动，这些均是房间对象的内部逻辑和操作。————class(绑定操作)
  step2: 判断数据是否需要永久存储——————>若需要，则加入文件读写
  step3: 看格式：是否需要将数据展示给人看？还是只需要电脑读懂即可？
         需要展示给人：文本文件/不需要：二进制文件（读写）。
  step4:是否要求与人交互——————>考虑设置菜单。
