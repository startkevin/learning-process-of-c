算法学习笔记day1~
chapter1：三种排序

一、<插入排序>
排序方法综述： 从最开始的n个数中先逐一比较找出最小的数字（特别注意只需要标记其角标索引即可），交换它与a[0]的数据
              第二次再在剩下的n-1个数中再找出最小的，与第二位的a[1]交换数值....
              以此类推....但注意：1.只需要进行一共n-1轮的比较查找即可  2.每次只在开头更新角标，过程中也只查找角标。
代码实现：
for(int i=0;i<n-1;i++){//一共有n个数字，只需要做n-1轮交换即可
   min_index=i;//注意在每轮开始的时候更新角标索引为确定后的下一项,即i
   for(int j=i;j<n;j++){//逐一对后面的数字进行最小值查找,注意区分！这是每次起始地的j为i。
       if(a[j]<a[min_index]){
          min_index=j;//不断更新最小值角标
      }
  }//一轮最小值查找结束，调整其在数组中的顺序
 int temp=a[min_index]；
 a[min_index]=a[i];
 a[i]=temp;
}

二、<冒泡排序>
排序方法综述：与选择排序有异曲同工的感觉，选择排序是一轮轮筛查选择每一个范围中的最小值，并将其提到最前方。
             而冒泡排序则是将每一轮新范围中的最大值向后移动，每次从i=0的第一项开始，和自己后面的邻项相比，
             更大就往后，就这一样一路将每次选出来的最大值向后移动，而轻的项则向泡泡一样往前冒。
代码实现：
for(int i=0;i<n-1;i++){//一共n个数依旧只需要n-1轮！
    for(int j=0;j<n-1-j){//注意这时每轮确定的是后方的大数，因此改变的是每次j的截至，为n-1-i
        if(a[j]>a[j+1]){//遇见比自己小的就继续向后移动交换
           int temp =a[j+1];
           a[j+1] = a[j];
           a[j] = temp;//需赋值三次
        }
    }
}

三、<插入排序>（理解起来好像要更难一点点-^-）
排序方法综述：num1.感觉上优先确定一个位于左侧的小值区间（有序区间,数组靠右为较大数？乱序的），左侧区间长度由短逐渐变长，最开始长度为i-1(1-1=0),
                  在后面的乱序数字逐次抽出一张（每次抽一张），通常第一次抽出的为第二张（a[1]）,将该张在前面有序区间由后至前比较（区间0~i-1）
                  在有序区间内遇到第一个比抽出牌数字大的就知道把抽出的牌（key）放在这张前面，后面的都依次向后挪动即可。
